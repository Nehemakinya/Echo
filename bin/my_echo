#!/usr/bin/env python



import sys
import os

# Add the path to the lib directory to the Python path
lib_path = os.path.join(os.path.dirname(__file__), '../lib')
sys.path.append(lib_path)

# import the echo function from echo_lib
from echo_lib import echo

args = sys.argv[1:]
echo(" ".join(args))
